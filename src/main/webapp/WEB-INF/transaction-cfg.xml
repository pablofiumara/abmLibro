<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xmlns:tx="http://www.springframework.org/schema/tx"
   xmlns:aop="http://www.springframework.org/schema/aop"
   xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop-4.1.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx-4.1.xsd">

   <import resource="data-source-hiber-cfg.xml"/>
  <!--
  mi "transaction-manager" se llama "miTransactionManager" y
  evalua la sesión (relacionado con DAO) llamada "miSessionFactory", que se encuentra el archivo "dao-cfg.xml"
  de la misma carpeta.
  miSessionFactory no se importa de manera directa (como sí ocurre con data-source-hiber-cfg.xml) ni de manera indirecta
  (a través del archivo mencionado) pero se puede referir aquí, dentro de la configuración de las transacciones.
   -->
   <tx:annotation-driven proxy-target-class="true" transaction-manager="miTransactionManager"/>
   <bean id="miTransactionManager"
       class="org.springframework.orm.hibernate4.HibernateTransactionManager">
       <property name="sessionFactory" ref="miSessionFactory"/>
   </bean>
</beans>